"""Domain logic: hashing, file classification, EXIF, AudioMoth, QC, inventory.

Pure-Python (plus Pillow/ExifTool for EXIF). No Qt and no Box imports, so every
module here is unit-testable in isolation.
"""
