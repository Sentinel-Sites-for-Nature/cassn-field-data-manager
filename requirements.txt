# CA-SSN Field Data Manager — Python dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: PyExifTool is a wrapper around the `exiftool` command-line tool, which
# must be installed separately:
#   macOS:   brew install exiftool
#   Windows: download from https://exiftool.org and add it to PATH
#
# The SoundHub pipeline also needs the `flac` encoder on PATH:
#   macOS:   brew install flac
#   Windows: download from https://xiph.org/flac and add it to PATH

PySide6==6.11.2          # Qt GUI
box-sdk-gen==1.17.0      # Box cloud storage + OAuth
boto3==1.43.86           # Wildlife SoundHub S3 upload
Pillow==12.3.0           # image EXIF reading
piexif==1.1.3            # image EXIF reading
PyExifTool==0.5.6        # camera metadata (requires the exiftool binary, see note above)
openpyxl==3.1.5          # data-collection summary XLSX reports and WI tracker imports
